{
  "name": "state-lookup",
  "version": "0.1.0",
  "description": "Lambda function code for state-lookup (lat/lng -> state/province + distance). Binary runtime: Flatbush index + geometry buffers. Boundary data ships in a layer.",
  "main": "index.js",
  "dependencies": {
    "flatbush": "^4.6.2"
  }
}
